<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shortner</title>
  </head>
  <body>

    <% if (locals.error) { %>
    <p class="error"><%= error %></p>
    <% } %>

    <form action="/" method="POST">
      <input placeholder="Enter your long url here"  type="url" name="url" id="url" />
      <input type="submit" value="Shorten!!" />
    </form>

    <div class="short-url">
      <% if (locals.short_url) { %>
      <a href="<%= short_url %>"><%= short_url %> </a>
      <% } %>
    </div>
  </body>
</html>